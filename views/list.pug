extends ./layout.pug

block title
  //- title !{home.getText("home.title")}

block body
  body.page

    div.home
      //- h1.title !{home.getText('home.title')}
    
      section.main
        for album in albums
          include partials/post.pug
